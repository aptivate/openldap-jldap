<!-- ======================================================================= -->
<!-- ORG.IETF.ASN1 build file                                                -->
<!-- ======================================================================= -->
<project name="ASN1 LDAP Classes" default="all" basedir=".">

	<property name="src.dir" value="${basedir}"/>
	<property name="top.dir" value="../../../../.."/>
	<property name="classes.dir" value="${top.dir}/classes"/>
	<property name="debug" value="on"/>
	<property name="optimize" value="on"/>


    <!-- =================================================================== -->
    <!-- initialization                                                      -->
    <!-- =================================================================== -->
    <target name="init">
        <tstamp/>
		<mkdir dir="${classes.dir}"/>
    </target>

    <!-- =================================================================== -->
    <!-- all task (default                                                   -->
    <!-- =================================================================== -->
    <target name="all" depends="init, compile"/>

    <!-- =================================================================== -->
    <!-- compile java files                                                  -->
    <!-- =================================================================== -->
    <target name="compile" depends="init">
        <javac srcdir="${src.dir}"
                destdir="${classes.dir}"
                debug="${debug}"
                deprecation="off"
                optimize="${optimize}">
                <classpath>
					<pathelement location="${top.dir}/../external/ssl/domestic/nssl1.2_dom.jar"/>
					<pathelement location="${top.dir}/../lib/asn1.jar"/>
				</classpath>
            <include name="*.java"/>
        </javac>
    </target>
</project>
