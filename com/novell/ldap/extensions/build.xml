<!-- ======================================================================= -->
<!-- COM.NOVELL.LDAP.EXTENSIONS build file                                                -->
<!-- ======================================================================= -->
<project name="ext" default="compile" basedir=".">
	<!-- Targets                                                             -->
	<!--     clean   - Cleans all generated class files                      -->
	<!--     compile - Builds debug classes and jar files                    -->
	<!--     all     - Synonym for compile                                   -->

	<property name="top" value="../../../.."/>
	<property name="ldap" value="ldap"/>
    <property name="build" value="classes_debug"/>
    <property name="lib" value="lib_debug"/>
	<property name="optimize" value="off"/>
	<property name="debug" value="on"/>


    <!-- =================================================================== -->
    <!-- initialization                                                      -->
    <!-- =================================================================== -->
    <target name="init">
        <tstamp/>
    </target>


    <!-- =================================================================== -->
    <!-- clean class files                                                  -->
    <!-- =================================================================== -->
    <target name="clean" depends="init">
		<delete dir="${top}/${build}/com/novell/ldap/extensions" includes="*.class"/>
	</target>

    <!-- =================================================================== -->
    <!-- compile java files                                                  -->
    <!-- =================================================================== -->
    <target name="compile" depends="init">
		<mkdir dir="${top}/${build}"/>
        <javac srcdir="${top}"
				includes="com/novell/ldap/extensions/*.java"
                destdir="${top}/${build}"
                deprecation="off"
                debug="${debug}"
                optimize="${optimize}">
        </javac>
    </target>

	<target name="all" depends="compile"/>

</project>
